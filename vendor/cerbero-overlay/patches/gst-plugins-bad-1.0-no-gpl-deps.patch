--- a/recipes/gst-plugins-bad-1.0.recipe
+++ b/recipes/gst-plugins-bad-1.0.recipe
@@ -545,14 +545,7 @@
             self.meson_options['svtav1'] = 'disabled'
             # universal archs cannot have different deps
             self.deps.append('svt-av1')
-        else:
-            self.enable_plugin('svtav1', 'codecs', dep='svt-av1')
-
-        self.enable_plugin('x265', 'codecs_gpl_restricted', dep='x265')
-
-        if not Platform.is_app_platform(self.config.target_platform):
-            # These need plugins
-            self.enable_plugin('frei0r', 'effects', dep='frei0r-plugins')
+        # Keep the audited closure free of optional codec dependencies.

         if self.config.target_platform == Platform.WINDOWS:
             self.meson_options['wasapi'] = 'enabled'
